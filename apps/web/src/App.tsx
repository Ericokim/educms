import { Button } from '@/components/ui/button'

function App() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-4 bg-background p-8">
      <h1 className="text-3xl font-semibold tracking-tight">EduCMS</h1>
      <p className="text-muted-foreground">
        Educational Content Management System — admin panel coming in the next phases.
      </p>
      <Button>Project setup complete</Button>
    </main>
  )
}

export default App
